<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= book.title %> - Comments</title>
  <link rel="icon" type="image/png" href="/images/favicon.ico">
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  

  <main class="comments-container">
    <h2>💬 Comments for <%= book.title %></h2>

    <% if (comments.length === 0) { %>
      <p>No comments yet. Be the first to comment!</p>
    <% } else { %>
      <ul class="comment-list">
        <% comments.forEach(comment => { %>
          <li class="comment-item">
            <a href="/users/<%= user.username %>">
              <img
              src="<%= comment.profile_image || '/images/default-avatar.png' %>"
              class="comment-avatar"
              alt="User Avatar"
            />
            </a>
            <div>
              <strong><%= comment.username %></strong><br />
              <span><%= comment.content %></span><br />
              <small><%= new Date(comment.created_at).toLocaleString() %></small>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } %>

    <% if (user) { %>
      <form action="/books/<%= book.id %>/comments" method="POST" class="comment-form">
        <textarea name="text" placeholder="Write your comment..." required></textarea><br />
        <button type="submit">Post Comment</button>
      </form>
    <% } else { %>
      <p><a href="/login">Login</a> to leave a comment.</p>
    <% } %>

    <a href="/" class="back-link">← Back to Home</a>
  </main>
</body>
</html>
